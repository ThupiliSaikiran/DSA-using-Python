Array :
An array is a collection of elements of the same data type stored in contiguous memory locations, which allows each element to be accessed directly using its index or position.


Homogeneous arrays:
it will contain the elements of same data type.

Heterogeneous arrays:
it will contain elements that may not be same data type